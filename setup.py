# setup.py
from setuptools import setup

setup(
    name='geomech',
    version='0.1.0',
    packages=['geomech'],
    install_requires=[
        "scipy", 
        "matplotlib",
        "numpy",
    ],
)
